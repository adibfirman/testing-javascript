const name = 'Adib'
typeof name === 'string'

if 
      (
                !(
                            'serviceWorker' in navigator)) {
  // you have an old browser
}

;[(1, 2, 3)].forEach(n => console.log(n))
